$(document).ready((function() {

    var screenHeight = window.innerHeight;
    $('.panel-resume0').css("margin-top", ((screenHeight-300)/2-50));

    $('.panel-resume0').stop().fadeTo(500, 1);
    $('.panel-resume1').stop().fadeTo(500, 1);

}));
